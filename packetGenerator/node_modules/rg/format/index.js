'use strict';

let packets = require('./packets');
let as3 = require('./as3');
let helper = require('./helper');

module.exports =
{
	generatePackets : packets.generatePackets,
	generateAs3Classes : as3.generatePackets,

	writeBuffer : helper.writeBuffer,
	writeHeader : helper.writeHeader,
	readBuffer : helper.readBuffer,
	readMask : helper.readMask,
	readHeader : helper.readHeader,
	generateRandom : helper.generateRandom,
	dataRawToStruct : helper.dataRawToStruct,
	dataStructToRaw : helper.dataStructToRaw,
	debugPosition : helper.debugPosition,
	cachePacket : helper.cachePacket,
	formats : helper.formats
};